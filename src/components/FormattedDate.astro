---
export interface Props {
	date: Date
}

const { date } = Astro.props
---

<time class="text-md text-black text-opacity-70" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
